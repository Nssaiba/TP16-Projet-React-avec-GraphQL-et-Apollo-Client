// Note: Ces interfaces sont fournies pour documentation.
// Dans un projet JavaScript classique, elles sont commentées pour éviter les erreurs de syntaxe.

/*
export interface Compte {
  id: string;
  solde: number;
  dateCreation: string;
  type: string;
}

export interface Transaction {
  id: string;
  type: string;
  montant: number;
  date: string;
  compte: Compte;
}

export interface SoldeStats {
  count: number;
  sum: number;
  average: number;
}

export interface TransactionStats {
  count: number;
  sumDepots: number;
  sumRetraits: number;
}

export interface CompteRequest {
  solde: number;
  type: string;
}

export interface TransactionRequest {
  type: string;
  montant: number;
  compteId: string;
}
*/
